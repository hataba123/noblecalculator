import { calculateCac, cacConfig, cacSeoContent, type CacInput, type CacResult } from "@noblecalculator/calculators-core/cac";

export { calculateCac, cacConfig, cacSeoContent };
export type { CacInput, CacResult };
