import { calculateMarkup, markupConfig, markupSeoContent, type MarkupInput, type MarkupResult } from "@noblecalculator/calculators-core/markup";

export { calculateMarkup, markupConfig, markupSeoContent };
export type { MarkupInput, MarkupResult };
